export default function Home() {
  return (
    /** A ideia é que essa página seja a principal do site, onde os usuários comuns vejam as infos sobre o vestibular
     * para que o admin veja o dashboard, ele deve acessar a rota /dashboard
     * dessa rota, ele será redirecionado para a página de login, onde ele deverá se autenticar
     *
     * Como essa página é construída por vários componentes, cada seção será um componente separado
     * importado aqui para compor a página principal e na parte do admin para edição
     */
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
    </main>
  );
}
